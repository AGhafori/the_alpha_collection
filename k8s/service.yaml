apiVersion: v1
kind: Service
metadata:
  name: alpha-collection
spec:
  type: NodePort
  selector:
    app: alpha-collection
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
